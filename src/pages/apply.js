import React from "react"
import Layout from "../components/layout"
import SEO from "../components/seo"

const Apply = () => (
  <Layout>
    <SEO title="Apply" keywords={[`gatsby`, `application`, `react`]} />
    <div className="apply">
      <h1>From Apply</h1>
    </div>
  </Layout>
)

export default Apply
